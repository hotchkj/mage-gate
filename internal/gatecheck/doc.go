// Vision: Pure gatecheck math/string parsing only—callers own subprocesses and IO; this package stays side-effect free.

// Package gatecheck provides pure logic functions for quality gate checks.
// These functions parse tool output and apply thresholds without subprocess calls.
package gatecheck
