#!/bin/sh
cd "$(git rev-parse --show-toplevel)" || exit 1
MAGE="go run github.com/magefile/mage@v1.17.0"
# Apply formatters before the non-mutating gate (lint verify, deadcode, vet, compile, …).
$MAGE format || exit 1
exec $MAGE gate
